services:
  hadoop:
    image: bde2020/hadoop-namenode:latest
    container_name: hadoop
    ports:
      - "9870:9870" # Web UI NameNode
      - "9000:9000" # HDFS
    environment:
      - CLUSTER_NAME=test
